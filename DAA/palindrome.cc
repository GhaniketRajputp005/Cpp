#include<iostream>
using namespace std;

bool ispal(string str)
{
    int arr[str.size()];
    for(int i=0;i<str.size();i++)
    {
        int ctr=0;
       for(int j=0;j<str.size())
    }
}
int main()
{
    cout<<"Enter string\n";
    string str;
    cin>>str;
    bool ans;
    ans  =  ispal(str);
    return 0;
}